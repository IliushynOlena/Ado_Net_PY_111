<Window x:Class="_08_ExampleEntity.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_08_ExampleEntity"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Click="Button_Click">Load Fligts</Button>
            <Button Click="Button_Click_1">Load Clients</Button>
            <Button Click="Button_Click_2">Load Products</Button>
        </ToolBar>
        <DataGrid Name ="dataGrid"></DataGrid>

    </DockPanel>
    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <ListBox  x:Name="workerList"></ListBox>
        <ListBox x:Name="projectList" Grid.Column="1"></ListBox>
        <DataGrid Name="d"></DataGrid>
    </Grid>-->
</Window>
